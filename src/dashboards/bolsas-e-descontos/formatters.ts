// Formatação numérica centralizada em src/lib/formatters.ts — este arquivo
// existe só para preservar os imports relativos deste dashboard.
export { fmtInt, fmtIntCompact, fmtBRL, fmtBRLCompact, truncateLabel } from '@/lib/formatters';
